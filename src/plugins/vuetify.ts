import { aliases, mdi } from "vuetify/iconsets/mdi-svg";
import type { IconOptions } from "vuetify/lib/framework.mjs";

export const icons: IconOptions = {
  defaultSet: "mdi",
  aliases,
  sets: {
    mdi,
  },
};
